# Agents Directory

Table of contents only — see each file for the full agent definition.

| Agent | Purpose | File |
|---|---|---|
| Planner | Turns a feature spec into a `docs/plans/<feature>.md` execution plan | `.claude/agents/planner.md` |
| Reviewer | Reviews a feature branch diff against its spec before merge | `.claude/agents/reviewer.md` |
| Janitor | Entropy management (stale locks, idle applications, shipped plans) | `.claude/agents/janitor.md` |
