services:
  mongo:
    image: mongo:7
    container_name: loanforge-mongo
    restart: unless-stopped
    ports:
      - "27117:27017"
    volumes:
      - loanforge-mongo-data:/data/db

volumes:
  loanforge-mongo-data:
